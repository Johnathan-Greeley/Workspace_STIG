def V220986(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220986"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   
def V220987(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220987"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220988(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220988"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220989(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220989"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220990(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220990"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220991(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220991"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220994(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220994"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220995(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220995"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220996(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220996"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220997(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220997"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220998(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220998"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V220999(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-220999"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221000(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221000"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221001(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221001"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
 
   
   def V221002(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221002"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221003(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221003"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221004(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221004"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221005(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221005"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221006(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221006"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221007(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221007"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221008(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221008"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221009(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221009"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221010(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221010"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221011(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221011"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221012(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221012"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221013(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221013"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221014(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221014"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221015(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221015"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221016(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221016"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221017(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221017"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   
def V221018(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221018"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221019(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221019"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221020(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221020"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221021(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221021"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221022(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221022"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221023(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221023"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221024(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221024"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221025(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221025"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221026(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221026"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221027(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221027"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221028(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221028"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221029(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221029"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221030(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221030"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221031(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221031"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221032(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221032"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221033(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221033"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221034(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221034"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221035(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221035"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221036(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221036"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221037(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221037"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221038(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221038"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221039(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221039"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221040(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221040"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221041(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221041"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221042(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221042"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221043(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221043"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221044(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221044"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221045(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221045"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221046(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221046"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221047(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221047"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   
def V221048(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221048"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221049(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221049"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221050(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221050"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221051(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221051"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221052(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221052"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221053(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221053"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221054(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221054"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221055(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221055"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221056(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221056"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221057(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221057"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221058(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221058"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221059(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221059"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221060(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221060"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221061(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221061"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221062(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221062"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221063(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221063"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221064(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221064"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221065(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221065"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221066(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221066"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221067(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221067"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221068(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221068"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V221069(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-221069"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237750(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237750"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237752(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237752"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237756(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237756"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237759(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237759"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237762(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237762"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237764(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237764"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237766(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237766"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   
   def V237772(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237772"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   def V237774(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237774"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   def V237776(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237776"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
   def V237778(devicetype, devicename):
   #Info about Vulnerability.
   check = Stig()
   #The vulnerability ID MUST match what the stig file has.  We're going to search the .ckl for it.
   check = Stig()
   check.vulid = "V-237778"
   check.status = "Not_Reviewed"
   strCommand = "sh run| i aaa authentic"
   #if devicetype == "NXOS":
   #    strCommand = "sh run | i \"aaa authentic\""
   strResult = ExecCommand(strCommand, devicename)
   check.finding = strResult
   #Time to check the results of the command...   save check status to check.status and commants to check.comments
   #if len(strResult1.splitlines()) < 3 and len(strResult2.splitlines()) < 3:
	#	check.status = "NotAFinding"
	#	check.comments = "NAF - There are no community or user accounts."
   #if strResult.find("logging level", len(devicename + "#" + strCommand)) > -1:
   #        check.status = "NotAFinding"
   #        check.comments = "NXOS logging enabled."
   #
   return check
  
  
  
  
  
  
  
                stigList.append(V220987(strDeviceType, strDeviceName.strip()))
                stigList.append(V220988(strDeviceType, strDeviceName.strip()))
                stigList.append(V220989(strDeviceType, strDeviceName.strip()))
                stigList.append(V220990(strDeviceType, strDeviceName.strip()))
                stigList.append(V220991(strDeviceType, strDeviceName.strip()))
                stigList.append(V220994(strDeviceType, strDeviceName.strip()))
                stigList.append(V220995(strDeviceType, strDeviceName.strip()))
                stigList.append(V220996(strDeviceType, strDeviceName.strip()))
                stigList.append(V220997(strDeviceType, strDeviceName.strip()))
                stigList.append(V220998(strDeviceType, strDeviceName.strip()))
                stigList.append(V220999(strDeviceType, strDeviceName.strip()))
                stigList.append(V221000(strDeviceType, strDeviceName.strip()))
                stigList.append(V221001(strDeviceType, strDeviceName.strip()))
                stigList.append(V221002(strDeviceType, strDeviceName.strip()))
                stigList.append(V220986(strDeviceType, strDeviceName.strip()))
                stigList.append(V221003(strDeviceType, strDeviceName.strip()))
                stigList.append(V221004(strDeviceType, strDeviceName.strip()))
                stigList.append(V221005(strDeviceType, strDeviceName.strip()))
                stigList.append(V221006(strDeviceType, strDeviceName.strip()))
                stigList.append(V221007(strDeviceType, strDeviceName.strip()))
                stigList.append(V221008(strDeviceType, strDeviceName.strip()))
                stigList.append(V221009(strDeviceType, strDeviceName.strip()))
                stigList.append(V221010(strDeviceType, strDeviceName.strip()))
                stigList.append(V221011(strDeviceType, strDeviceName.strip()))
                stigList.append(V221012(strDeviceType, strDeviceName.strip()))
                stigList.append(V221013(strDeviceType, strDeviceName.strip()))
                stigList.append(V221014(strDeviceType, strDeviceName.strip()))
                stigList.append(V221015(strDeviceType, strDeviceName.strip()))
                stigList.append(V221016(strDeviceType, strDeviceName.strip()))
                stigList.append(V221017(strDeviceType, strDeviceName.strip()))
                stigList.append(V221018(strDeviceType, strDeviceName.strip()))
                stigList.append(V221019(strDeviceType, strDeviceName.strip()))
                stigList.append(V221020(strDeviceType, strDeviceName.strip()))
                stigList.append(V221021(strDeviceType, strDeviceName.strip()))
                stigList.append(V221022(strDeviceType, strDeviceName.strip()))
                stigList.append(V221023(strDeviceType, strDeviceName.strip()))
                stigList.append(V221024(strDeviceType, strDeviceName.strip()))
                stigList.append(V221025(strDeviceType, strDeviceName.strip()))
                stigList.append(V221026(strDeviceType, strDeviceName.strip()))
                stigList.append(V221027(strDeviceType, strDeviceName.strip()))
                stigList.append(V221028(strDeviceType, strDeviceName.strip()))
                stigList.append(V221029(strDeviceType, strDeviceName.strip()))
                stigList.append(V221030(strDeviceType, strDeviceName.strip()))
                stigList.append(V221031(strDeviceType, strDeviceName.strip()))
                stigList.append(V221032(strDeviceType, strDeviceName.strip()))
                stigList.append(V221033(strDeviceType, strDeviceName.strip()))
                stigList.append(V221034(strDeviceType, strDeviceName.strip()))
                stigList.append(V221035(strDeviceType, strDeviceName.strip()))
                stigList.append(V221036(strDeviceType, strDeviceName.strip()))
                stigList.append(V221037(strDeviceType, strDeviceName.strip()))
                stigList.append(V221038(strDeviceType, strDeviceName.strip()))
                stigList.append(V221039(strDeviceType, strDeviceName.strip()))
                stigList.append(V221041(strDeviceType, strDeviceName.strip()))
                stigList.append(V221042(strDeviceType, strDeviceName.strip()))
                stigList.append(V221043(strDeviceType, strDeviceName.strip()))
                stigList.append(V221044(strDeviceType, strDeviceName.strip()))
                stigList.append(V221045(strDeviceType, strDeviceName.strip()))
                stigList.append(V221046(strDeviceType, strDeviceName.strip()))
                stigList.append(V221047(strDeviceType, strDeviceName.strip()))
                stigList.append(V221048(strDeviceType, strDeviceName.strip()))
                stigList.append(V221049(strDeviceType, strDeviceName.strip()))
                stigList.append(V221050(strDeviceType, strDeviceName.strip()))
                stigList.append(V221051(strDeviceType, strDeviceName.strip()))
                stigList.append(V221052(strDeviceType, strDeviceName.strip()))
                stigList.append(V221053(strDeviceType, strDeviceName.strip()))
                stigList.append(V221054(strDeviceType, strDeviceName.strip()))
                stigList.append(V221055(strDeviceType, strDeviceName.strip()))
                stigList.append(V221056(strDeviceType, strDeviceName.strip()))
                stigList.append(V221057(strDeviceType, strDeviceName.strip()))
                stigList.append(V221058(strDeviceType, strDeviceName.strip()))
                stigList.append(V221059(strDeviceType, strDeviceName.strip()))
                stigList.append(V221060(strDeviceType, strDeviceName.strip()))
                stigList.append(V221061(strDeviceType, strDeviceName.strip()))
                stigList.append(V221062(strDeviceType, strDeviceName.strip()))
                stigList.append(V221063(strDeviceType, strDeviceName.strip()))
                stigList.append(V221064(strDeviceType, strDeviceName.strip()))
                stigList.append(V221065(strDeviceType, strDeviceName.strip()))
                stigList.append(V221066(strDeviceType, strDeviceName.strip()))
                stigList.append(V221067(strDeviceType, strDeviceName.strip()))
                stigList.append(V221068(strDeviceType, strDeviceName.strip()))
                stigList.append(V221069(strDeviceType, strDeviceName.strip()))
                stigList.append(V237750(strDeviceType, strDeviceName.strip()))
                stigList.append(V237752(strDeviceType, strDeviceName.strip()))
                stigList.append(V237756(strDeviceType, strDeviceName.strip()))
                stigList.append(V237759(strDeviceType, strDeviceName.strip()))
                stigList.append(V237762(strDeviceType, strDeviceName.strip()))
                stigList.append(V237764(strDeviceType, strDeviceName.strip()))
                stigList.append(V237766(strDeviceType, strDeviceName.strip()))
                stigList.append(V237772(strDeviceType, strDeviceName.strip()))
                stigList.append(V237774(strDeviceType, strDeviceName.strip()))
                stigList.append(V237776(strDeviceType, strDeviceName.strip()))
                stigList.append(V237778(strDeviceType, strDeviceName.strip()))